import 'package:flutter/material.dart';

class ComboScreen extends StatelessWidget {
  const ComboScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Combo Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
